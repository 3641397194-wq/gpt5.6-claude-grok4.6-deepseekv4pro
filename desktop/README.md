# 冷咖啡 ColdBrew Zero Desktop

完整桌面工作台：启动页、指挥台、五档工作档位、五个 BREAK//OPEN 通道、四个模型席位、明确目标输入、活动流、结果面板、隔离状态与社群入口。

## 启动

```powershell
npm install
npm start
```

打包 Windows 便携版：

```powershell
npm run pack:win
```

桌面版只处理用户在当前输入框中明确提交的文本。它不会读取本机文件、目录、配置、剪贴板、环境变量或聊天历史。品牌启动词是 `冷咖啡`，新版控制词是 `BREAK//OPEN`。

